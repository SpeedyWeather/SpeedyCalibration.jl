# Changelog

## Unreleased

- Initial public release: parameter specification (`ParamSpec`), loss configuration
  (`LossConfig`, OSR/SRU/SRD, Trenberth), online-gradient training loop (`calibrate!`),
  climate validation (`run_climate_validation`), diagnostics (`NaNWatchCallback`,
  `check_nans`, `vertical_gradient_anomalies`), reference-data comparison utilities,
  and Makie-based plotting (training curves, climate maps, vertical profiles,
  NaN-watch diagnostics).
